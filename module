from odoo import models, fields, api
import json

class Projectiht(models.Model):
  _inherit = "project.project"

  lokasi_proyek = fields.Text( string = "Lokasi Proyek", help = "lokasi proyek")

  source_aplikasi_pendukung = fields.Char( string = "url", help = )
  
   daftar_perkerja_remote = fields.Json( string="Daftar Pekerja Remote", help = )
